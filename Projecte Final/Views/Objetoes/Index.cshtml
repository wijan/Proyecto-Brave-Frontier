@model IEnumerable<Projecte_Final.Models.Objeto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Crafteo.Craft)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Icona.FileName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NomObjeto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Crafteo.Craft)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Icona.FileName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NomObjeto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
